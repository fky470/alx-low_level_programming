#include "main.h"

/**
 * add - function that prints addition of two numbers
 * @x: takes in an integer
 * @y: takes in an integer
 * return: x + y result
 */
int add(int x, int y)
{
	return (x + y);
}
