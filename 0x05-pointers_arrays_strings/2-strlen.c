#include "main.h"
#include <stdio.h>

/**
 * _strlen - a function that returns the length of a string
 * @s: integer value
 * @i: takes in an integer
 * return: returns length
 */

int _strlen(char *s)
{
	int i;
	for (i = 0; s[i] != '\0'; i++)

	return (i);
}
